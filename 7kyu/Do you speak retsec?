https://www.codewars.com/kata/5516ab668915478845000780

function reverseByCenter(s){
  let m = Math.floor(s.length / 2);
  return s.length % 2 === 0 ? s.slice(m) + s.slice(0, m) : s.slice(m+1) + s[m] + s.slice(0, m);
}
